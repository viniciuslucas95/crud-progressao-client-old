<UserControl x:Class="crud_progressao.UserControls.MonthInputUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Content="Mês"
               HorizontalContentAlignment="Center"
               Style="{StaticResource SmallLabel}"
               Padding="0" FontSize="10"/>
        <TextBox x:Name="inputMonth" Grid.Row="1"
                 BorderThickness="0 1 0 0" FontSize="12"
                 TextAlignment="Center" Padding="0"/>
        <Label Content="Ano" Grid.Column="1" Padding="0"
               HorizontalContentAlignment="Center"
               Style="{StaticResource SmallLabel}"
               BorderThickness="1 0 0 0" FontSize="10"/>
        <TextBox x:Name="inputYear" Grid.Row="1" FontSize="12"
                 Grid.Column="1" BorderThickness="1 1 0 0"
                 TextAlignment="Center" Padding="0"/>
    </Grid>
</UserControl>
